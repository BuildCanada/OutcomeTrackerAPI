GEMINI_API_KEY=

# Hosted defuddle worker (content extraction). URL defaults to the production worker.
DEFUDDLE_API_KEY=
DEFUDDLE_API_URL=https://deffudler.svc.canadasbuilding.com

# Claude OAuth: pool JSON takes precedence over the optional single-token fallback.
# Tokens are runtime secrets. Use distinct accounts; tokens from one account share quota.
# CLAUDE_CODE_OAUTH_TOKENS={"account-a":"token-a","account-b":"token-b"}
CLAUDE_CODE_OAUTH_TOKEN=
